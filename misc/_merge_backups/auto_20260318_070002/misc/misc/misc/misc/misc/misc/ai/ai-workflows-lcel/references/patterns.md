# LCEL patterns

- snapshot -> diagnose -> plan -> act -> verify
- file -> parse -> retrieve -> synthesize
- input -> transform -> custom API -> normalize -> output
- keep each stage testable in isolation
