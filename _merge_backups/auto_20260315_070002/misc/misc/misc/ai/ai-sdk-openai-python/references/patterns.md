# Patterns

1. Probe `/models`.
2. Probe minimal `/chat/completions`.
3. Compare raw HTTP vs SDK.
4. Add browser-like headers only for diagnosis, not as magical fixes.
5. Distinguish endpoint class: OpenAI-native, OpenAI-compatible, custom proxy.
